// useTheme lives in ../theme (the ThemeContext is defined there) — it is NOT
// re-exported here so the top-level barrel exports each name exactly once.
export { useComponentStyles } from './useComponentStyles';
